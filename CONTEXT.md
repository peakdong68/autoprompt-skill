# Autoprompt 范围档位（scope profile）与术语表

Autoprompt 是一套显式调用的编排循环：把一次使命（mission）变成唯一一份独立获批的可执行路线图，直接派发可实施的工作线，并用真实测试与独立评审证明交付行为。

本文档回答一个问题：`bounded` / `multi-surface` 到底是什么、用来干什么。

## 一、一句话

`bounded` / `multi-surface` 是 Autoprompt 给**单次使命**打的「编排规模等级」（scope profile）——它决定这次任务配几个子代理、跑几轮、要不要加侦察员、时间上限多少。本质是**算力预算档位**，不是任务难度的描述。

## 二、其实是三档，不是两档

| 档位 | 侦察员 | 总代理数 | 轮数 | 目标时长 | 门径路径 |
|---|---:|---:|---:|---|---|
| `bounded` | 0 | 3 | 2 | 不到 1 分钟 | 作者 → 评审 + 全新验证（并发）→ DONE |
| `multi-surface` | 2 | 5 | 3 | 不到 5 分钟 | 作者 → 两名侦察员（并发）→ 评审 + 全新验证（并发）→ DONE |
| `unusually-large` | 5 | 9 | 4 | — | 必须在路线图中记录具体升级理由 |

## 三、用处：它实际驱动了 6 件事

1. **决定扇出宽度**：门控按档位取出 `scouts` 数量再决定派几名侦察员；`bounded` 一名都不派，`multi-surface` 恰好两名。
2. **决定代理预算与轮数**：3 / 5 / 9 个代理、2 / 3 / 4 轮；另有「路线图最多 12 个条目」作为分解失控的成本护栏。
3. **决定校验强度**：`unusually-large` 若没在路线图里写出**非空的升级理由**，结构校验直接拒绝——高预算必须给理由。
4. **决定时间硬顶**：5 分钟（300 秒）是普通 `multi-surface` 的上限，监督进程到点强杀并转 RESUME。
5. **定义「最少编排」下限**：`bounded` 是**最低拓扑**。已调用的使命永远进入编排，主代理**不能**把它偷偷降级成直接执行；跳过或收窄派发必须显式写进运行账本并给出理由。
6. **作为字段写进 `ROADMAP.md`**：`scopeProfile` 取值为 `bounded` / `multi-surface` / `unusually-large`；未知取值会回落成 `multi-surface`。

配套红线：**绝不把多表面使命压缩成单条有界工作线来缩短路线图**；`bounded` 只有在使命**确实只有一个表面**时才成立。

## 四、谁决定、何时决定

- 由**第一位路线图作者**（`ap-scoper`）在做抱负分级时分类，并附上证据。
- 门控先给一个**临时档**（扩展型使命 → `multi-surface`，否则 `bounded`）；作者回报 `scopeProfile` 后覆盖该临时值。
- 若作者改判（且不是 `unusually-large`），已派出的侦察员会被清空并重新计算，避免按错误档位烧预算。

## 五、易混语义：三种 `bounded`

| 语义 | 含义 | 出现处举例 |
|---|---|---|
| ① 范围档位 | 本文档讨论的 `bounded`，3 代理 / 2 轮 | `SKILL.md` §3、`GATES.md`、`plan-scope.md` |
| ② 层级标签 | `T1 bounded`，与 T0/T2/T3 并列的任务规模标签 | `PLAYBOOKS.md` L7 |
| ③ 普通形容词 | 「有限的、自足的」：有界批次、有界工作线、有界读取职责、等待是有界的 | `ap-researcher.md`、`composition.md`、`GATES.md` L81 |

三者都与档位无关，改档位时不要误改 ②③。

## 六、权威位置（按层）

| 层 | 文件 |
|---|---|
| 契约 | `SKILL.md` §3、`MODES.md` L16–25、`PLAYBOOKS.md` L15–16、`GATES.md` L36–37 |
| 流程 | `frameworks/plan-scope.md` L11 / L16 / L36 / L40 |
| 人格 | `agents/ap-scoper.md`（分类者）、`agents/ap-scope-coordinator.md` L26–31（派发者）、`agents/ap-manager.md` L43 |
| 执行 | `agents/claude/workflow/autoprompt-gate.js` L3030–3034（真值表与校验）、`phase-budget.js` L21 与 `supervisor.sh`（时间硬顶） |

**注意**：`autoprompt-gate.js` **只存在于 `agents/claude/workflow/`**。`agents/deepseek/` 下没有任何 `workflow/` 目录，也没有任何 `.js`，其范围档位只有**散文契约**（靠人格指令自律），没有机器强制的门控脚本。

## 七、已知文档与代码不一致

文档对 `unusually-large` 只写「超过 6-agent 普通预算才可」，**没写具体拓扑**；代码里它被钉死为 5 名侦察员 / 9 个代理 / 4 轮。要改这一档只能改代码里的真值表，改文档不会生效。

## Language（术语表）

> 词条保留代码中逐字出现的英文标识，中文注解以括号附于词头。

### 范围档位（Scope profile）

**Scope profile**（范围档位）:
任务在进入构建前被赋予的编排规模等级；它决定派发的扇出宽度，以及这项任务要为独立保证支付多少成本。
_避免_: 范围类型、复杂度等级、任务规模

**bounded**（有界档）:
最低一档的编排规模——一位路线图作者，随后独立评审者与盲测全新验证者并发执行——也是任何已调用任务的编排下限，仅当使命确实只有一个表面（surface）时才成立。切勿与 `T1 bounded` 层级标签、或普通形容词义（有限/自足）混为一谈。
_避免_: 小任务、简单任务、单表面任务

**multi-surface**（多表面档）:
用于存在真正互不重叠表面的使命的编排规模：保留作者的完整路线图与证据，恰好增加两名互补侦察员（scout）并发执行，随后独立评审与盲测全新验证并发执行。此类使命绝不能被压缩成单条有界工作线。
_避免_: 多部分任务、庞大任务、复杂任务

**unusually-large**（超常规模档）:
超出普通代理预算的升级档位；只有在运行账本中记录了具体的升级理由时才有效。
_避免_: 巨大、史诗级、下一阶段

**surface**（表面）:
交付物中真正独立的一部分，可以被单独拥有、构建与验证。
_避免_: 区域、模块、组件、子系统

**lane**（工作线）:
一条互不重叠的工作单元，只有唯一的具名所有者。
_避免_: 轨道、工作流、线程

**scout**（侦察员）:
互补型仓库情报工作者，只负责一个互不重叠的主题，返回证据与修正建议，且绝不单独撰写范围产物。
_避免_: 研究员、探索者、调研员

**round**（轮）:
一次派发波次：一个就绪组内的全部派发，都在收集任何报告之前发出。
_避免_: 遍、迭代、周期

### 任务分级（Mission sizing）

**tier**（层级标签）:
任务规模标签——`T0` trivial（琐碎）、`T1` bounded（有界）、`T2` feature（特性）、`T3` ambitious（宏大）——与范围档位一并记录，但二者相互独立。
_避免_: 等级、类别、大小、优先级
