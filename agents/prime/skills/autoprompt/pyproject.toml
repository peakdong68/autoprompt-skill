[project]
name = "autoprompt-prime-dispatch"
version = "2.0.0"
description = "Retired v1 dispatcher entry for the Autoprompt v2 controller"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/autoprompt"]
